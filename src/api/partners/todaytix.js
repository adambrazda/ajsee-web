// /src/api/partners/todaytix.js

// Později tady bude fetch na TodayTix API
export async function fetchTodayTixEvents({ locale, filters }) {
  // return fetch(...) => převedeš na univerzální event
  return [];
}
